# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

find_package(Torch REQUIRED)

include(midasTaskLib)
ADD_LIBRARY(${MIDAS_TASKLIB_NAME} SHARED
    ${MIDAS_TASKLIB_SOURCES})
add_dependencies(${MIDAS_TASKLIB_NAME}
    regen-typekit)



TARGET_LINK_LIBRARIES(${MIDAS_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${QT_LIBRARIES}
    ${TORCH_LIBRARIES}
    ${MIDAS_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${MIDAS_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${MIDAS_TASKLIB_INTERFACE_LIBRARIES}")
SET_TARGET_PROPERTIES(${MIDAS_TASKLIB_NAME}
    PROPERTIES INTERFACE_LINK_LIBRARIES "${MIDAS_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${MIDAS_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${MIDAS_TASKLIB_HEADERS}
    DESTINATION include/orocos/midas)

